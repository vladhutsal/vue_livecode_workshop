import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      light: {
        background: '#CFD8DC',
        coxit: '#FFAB40',
        pastelOrange: '#FFE0B2',
        pastelRed: '#FFCCBC',
      },
    },
  }

});
